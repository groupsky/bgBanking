<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="eu.masconsult.bgbanking"
    android:versionCode="1"
    android:versionName="1.0" >
    
    <!-- We need this to be able to create accounts in AccountManager -->
    <uses-permission android:name="android.permission.AUTHENTICATE_ACCOUNTS"/>

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="15" />

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >

        <!-- The authenticator services -->
        <service
            android:name=".banks.procreditbank.AuthenticationService"
            android:exported="false"
            android:process=":auth" >
            <intent-filter>
                <action android:name="android.accounts.AccountAuthenticator" />
            </intent-filter>

            <meta-data
                android:name="android.accounts.AccountAuthenticator"
                android:resource="@xml/authenticator_probanking" />
        </service>
        <service
            android:name=".banks.dskbank.AuthenticationService"
            android:exported="false"
            android:process=":auth" >
            <intent-filter>
                <action android:name="android.accounts.AccountAuthenticator" />
            </intent-filter>

            <meta-data
                android:name="android.accounts.AccountAuthenticator"
                android:resource="@xml/authenticator_dskdirect" />
        </service>
    </application>

</manifest>